# フロントエンドおよびロジック担当エージェントへの指示書

現在、`feature/auto-yaku-detection` ブランチにて大規模な仕様変更の作業を開始しています。
ユーザーの要望により、手動での「役の選択（チェックボックス）」を廃止し、「手牌を一つ一つ選択して自動的に役を判定する」機能を追加します。

以下の分担で実装と改修を進めてください。

## 1. フロントエンド担当エージェントのタスク
- `index.html`, `style.css`, `script.js` の改修。
- 現在の「役の選択」セクション（チェックボックス等のUI）を**完全に削除**してください。
- 萬子・筒子・索子・字牌の各牌を視覚的にタップ（クリック）して選び、現在の手牌（13枚＋和了牌の計14枚など）を組み立てられる新しいUIコンポーネントを実装してください。
- プレミアムでモダンなデザイン（Glassmorphismなど）を維持しつつ、直感的で使いやすい牌選択インターフェースにしてください。
- 選択された手牌情報（配列など）をロジック側に渡し、計算結果を受け取って画面に表示するように連携部分を修正してください。

## 2. ロジック担当エージェントのタスク
- `logic.js` および `logic.test.js` の大幅な改修。
- ユーザーが選択した「手牌データの配列」と「各種条件（ツモ/ロン、鳴き、親/子、自風/場風など）」を入力として受け取り、**麻雀の役を自動的に判定するアルゴリズム**を新規に実装してください。
- 麻雀の手牌パターンのパースや待ちの判定、役の複合計算など非常に複雑なロジックとなります。十分な設計を行った上で、必ずテスト駆動開発（TDD）のアプローチを用いて進めてください。
- 既存の符計算や点数計算（ベースは30符等）のロジックは引き続き活用しつつ、自動判定された役と翻数に基づいて正しく最終スコアが出力されるように統合してください。

## 3. カン（槓）への対応タスク（追記）
現在の仕様では手牌が最大14枚に固定されているため、カン（暗槓・明槓）が発生した場合の手牌枚数の増加（1回のカンにつき+1枚、最大18枚）に対応できていません。
以下の対応を追加で行ってください。

### フロントエンド担当
- 手牌の最大枚数を14枚から可変（カン1回につき+1枚、最大18枚まで保持可能）に対応させてください。
- ユーザーが特定の同種牌を4枚選んだ際に「カン」として扱えるUI（例：4枚揃ったら「カン」ボタンが現れ、手牌領域で1つの面子としてグループ化される、あるいは「暗槓」「明槓」を明示できるトグルなど）を追加してください。

### ロジック担当
- `logic.parser.js` を改修し、手牌が14枚を超えるケースであっても「カン」が含まれている構造として正しくパースできるようにしてください。
- `logic.yaku.js` で「三槓子」「四槓子」などのカンに関連する役牌判定などが正しく行われるか確認・修正してください。
- 上記の対応について、テスト駆動開発（TDD）に基づき `logic.test.js` または `logic.parser.test.js` 等にエッジケースのテストを追加してください。

## 4. プロジェクト全体レビューに基づく不足機能の追加（最優先課題）
自動役判定のロジックやUIを検証した結果、現在「多数の通常役」および「外部環境の状況役」が未実装のままとなっています。
以下の実装を直ちに行ってください。

### ロジック担当（logic.yaku.js, logic.parser.jsの改修）
現状以下の役が漏れていますので、すべて正しく判定できるように `logic.yaku.js` を拡張してください。
- **1翻**: 一盃口
- **2翻**: 対々和、三暗刻、三色同刻、チャンタ、混老頭、小三元
- **3翻**: 純チャン、二盃口
- **状況役（引数等で付与）**: 嶺上開花、槍槓、海底摸月、河底撈魚、天和、地和
- **役満全般**: 四暗刻、大三元、各種役満など。※特に**国士無双**については元の `logic.parser.js` 全く解釈できないため、パーサーの根幹レベルで国士無双を判定できるロジックを必ず追加してください。

### フロントエンド担当（index.html, script.jsの改修）
ロジック担当が状況役や風の判定を行えるよう、以下のUI要素を追加し `calculateMahjongScore` のオプションペイロードへ渡るように改修してください。
- **状況役の追加**: 現在のUIの状況役チェックボックスに「嶺上開花（カン時のみ有効化等）」「槍槓」「海底/河底」「天和/地和」を追加してください。
- **風（自風・場風）の選択**: 現在ロジック内部で東や南に固定されてしまっているため、ユーザーが「場風」と「自風」をそれぞれセレクトボックスで選べるUIを追加してください。

---
> **注意**: セクション4の通常役・状況役・場風/自風はすでにコードレビュー担当エージェントが実装済みです。新たに追加の対応は不要です。

## 5. 役満の手牌判定および国士無双パーサー対応（最優先かつ最後の追加指示）

コードレビューの結果、現在すべての役満が**手牌の形からは自動判定されていない**ことが確認されました。
以下の実装をTDD（テスト駆動開発）に基づき行ってください。

### ロジック担当（logic.yaku.js, logic.parser.js の改修）

#### (A) logic.parser.js への国士無双パーサーの追加
現在の `parseHand()` 関数は「4面子+1雀頭」および「七対子」のみをサポートしますが、**国士無双（13種の端牌・字牌がすべて揃い、そのうち1種が対子になっている形）はこれで解釈できません**。

`parseHand()` または別の補助関数 `isKokushiMusou(handTiles)` を追加し、以下の仕様を実装してください：
- 手牌が `m1, m9, p1, p9, s1, s9, z1, z2, z3, z4, z5, z6, z7` の13種類を全て含む場合に `true` を返す
- さらに、うち1種が2枚（対子）になっていれば「十三面待ち」でも「単騎待ち」でも役満として判定する
- 返却値として、役満を示す特別な組み合わせオブジェクト `[{ type: 'kokushi', tiles: [...] }]` を返す（または専用フラグを付ける）

#### (B) logic.yaku.js への役満判定の追加
`evaluateYaku()` 関数に以下の役満判定を追加してください。

**通常役満（各13翻）:**
- **四暗刻（スーアンコウ）**: 4組すべてが暗刻（isNakiなし）で構成されている場合 ※単騎待ちでツモの場合はダブル役満として扱わないこと（このアプリでは役満固定で可）
- **大三元（ダイサンゲン）**: 白・發・中の3つすべての刻子を持つ
- **字一色（ツーイーソー）**: 手牌が全て字牌のみで構成されている
- **清老頭（チンロウトウ）**: 手牌が全て1・9牌のみで構成されている（字牌なし）
- **緑一色（リューイーソー）**: 手牌全てが索子の2・3・4・6・8または発のみで構成されている
- **国士無双（コクシムソウ）**: 上記 (A) のパーサーから判定結果を受け取り、役満としてリストに追加する
- **小四喜（ショウスーシー）**: 東・南・西・北のうち、3つが刻子（または槓子）で1つが対子
- **大四喜（ダイスーシー）**: 東・南・西・北の全てが刻子（または槓子）（ダブル役満 = 26翻 = han: 26 として扱ってください）
- **九蓮宝燈（チューレンポウトウ）**: 同一の数牌スートで 1112345678999 の形 ＋ 同スートの任意の1牌（複雑な場合は判定をスキップして構いません）

#### (C) テストの追加
`logic.yaku.test.js` に以下のテストを追加してください：
- 四暗刻の判定テスト（4つすべて暗刻）
- 大三元の判定テスト（白・發・中の刻子）
- 字一色の判定テスト（全字牌構成）
- 清老頭の判定テスト（全1・9牌構成）
- 緑一色の判定テスト（2・3・4・6・8・發のみ）
- 国士無双の判定テスト（`logic.parser.js` の isKokushiMusou() を用いる）
- 小四喜の判定テスト

#### (D) 役満の重複判定・特別処理
`evaluateHand()` 内（`logic.js`）で、集計した翻数が13翻以上の場合は点数を固定役満として計算するよう処理が既にありますが、**ダブル役満（26翻以上）には専用ブランチ（32000点の2倍）を追加してください**。

---
> **注意**: セクション5の役満判定はすでに実装済みです。国士無双の混老頭誤判定バグもコードレビュー担当が修正済みです。

## 6. UI改善：和了牌・待ちの種類の入力および鳴き詳細の入力（最優先課題）

ユーザーからの改善報告に基づき、以下の2つのUI改善とロジック修正を行ってください。

---

### 6-A. 和了牌（上がり牌）と待ちの種類の選択UI

#### 背景と目的
現在のロジックでは「待ちの種類（両面・嵌張・辺張・単騎・双碰）」が **常に両面（リャンメン）と仮定** されています（`options.isRyamen: true` として渡しています）。
しかし以下の役に直接影響します：
- **平和（ピンフ）**: リャンメン待ちでなければ成立しない
- **単騎待ちの符計算**: 将来の精密符計算に影響

#### フロントエンド担当（index.html, script.js）

**and: 手牌に入力済みの牌から「和了牌（あがり牌）」をユーザーが1枚指定できるUIを追加してください。**

UI仕様:
- 14枚目の牌を選択した後（手牌が完成したとき）、「和了牌（上がり牌）を指定する」モードに入ること。
- 最もシンプルな実装として「手牌の中の1枚をクリックして和了牌としてマークする」方式を推奨します。和了牌としてマークされた牌は視覚的に（例：枠が光る・角にアイコン）区別されるようにしてください。
- 和了牌がマークされたら、その牌の隣（またはモーダル形式）で **待ちの種類** をユーザーが選択できるUIを表示してください：
  - `両面（リャンメン）`
  - `嵌張（カンチャン）`
  - `辺張（ペンチャン）`
  - `単騎（タンキ）`
  - `双碰（シャンポン）`
- 選択された和了牌とその待ちタイプは `performCalculation()` のpayloadに追加してください：
  ```js
  winTile: '3m', // 指定された和了牌
  waitType: 'ryamen', // 'ryamen' | 'kanchan' | 'penchan' | 'tanki' | 'shanpon'
  ```

#### ロジック担当（logic.js, logic.yaku.js）

- `calculateMahjongScore(payload)` が `payload.winTile` と `payload.waitType` を受け取り、`options` に `waitType` を渡すよう修正してください。
- `evaluateYaku()` 内の平和（ピンフ）の判定を以下のように修正してください：
  ```js
  // 現在の誤った実装（常に両面と仮定）
  if (shuntsuCount === 4 && !isYakuhaiPair && options.isRyamen) { ... }
  
  // 正しい実装（ユーザー指定の待ちタイプを使う）
  if (shuntsuCount === 4 && !isYakuhaiPair && options.waitType === 'ryamen') { ... }
  ```
- `calculateMahjongScore` にわたっている `isRyamen: true` のハードコードを削除し、`payload.waitType === 'ryamen'` で置き換えてください。

---

### 6-B. 鳴き（副露）の詳細入力

#### 背景と目的
現在の実装では「鳴きあり」トグルがONの場合、手牌14枚すべてを通常通りパースしていますが、  
**実際には「チー」「ポン」「カン（大明槓）」によって副露面子の構成が異なり**、それが役の成立・点数に影響します（特に食い下がり処理など）。

また、現在は鳴き牌の面子がパーサーによって自動推定されていますが、カンと通常の4枚持ちを正確に区別するためにも、どの面子が副露なのかをユーザー入力に基づいて明示することが望ましいです。

#### フロントエンド担当（index.html, script.js）

「鳴き」トグルがONになったとき、鳴き面子を入力できるモーダルまたは副露エリアを表示してください：

**UI仕様:**
- 鳴きトグルをONにすると、「副露面子を追加」ボタンが表示される。
- ボタンを押すと副露面子の入力フォームが現れ、以下を入力できるようにする：
  1. **鳴きの種類**: `チー（順子）` / `ポン（刻子）` / `大明槓（明槓）`
  2. **構成牌**: 牌ピッカーから3枚（または4枚）を選択
- 入力された副露面子（複数可）を一覧で表示し、削除もできるようにする。
- 副露された面子も14枚の手牌に加算して管理すること（手牌スロットの一部として表示）。
- 副露面子の情報をpayloadに追加する：
  ```js
  nakedMelds: [
    { type: 'chii', tiles: ['3m', '4m', '5m'] },  // チー面子
    { type: 'pon', tiles: ['2z', '2z', '2z'] },    // ポン面子
  ]
  ```

#### ロジック担当（logic.js, logic.parser.js, logic.yaku.js）

- `payload.nakedMelds` を受け取り、`evaluateHand()` に渡してください。
- `parseHand()` は `nakedMelds` が渡された場合、それらをすでに確定された面子（minkan/pon/chii）として扱い、残りの牌だけを組み合わせてパースするよう改修してください。
- `evaluateYaku()` では面子の `naki` フラグを正確に使用して、食い下がり・門前役除外を行ってください。

---

### 6-C. テストの追加
- 嵌張待ちで平和にならないテスト（`waitType: 'kanchan'` → ピンフ不成立）
- 副露（ポン面子あり）で三暗刻不成立になるテスト
- 副露（チー面子あり）で門前清自摸和不成立になるテスト
